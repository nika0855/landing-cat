

mixin list(items, className, active)
	ul(class=className)
		each item, index in items
			li(class=className + '__item')
				if index === active
					span(class=className + '__active')=item
				else
					a(class=className + '__link')=item

+list(['Home', 'About us', 'Our Approch', 'Our Work'], 'list', 0) 

mixin article(title)
	.article
		.article-wrapper
			h1= title
			if block
				block
			else
				p No content provided

+article('Hello world')

+article('Hello world')
	p This is my
	p Amazing article